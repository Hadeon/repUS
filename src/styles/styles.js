import React from 'react';
import {
  StyleSheet
} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        margin: 20
    },
    largeHeader: {
        textAlign: 'center',
        fontSize: 25,
        fontWeight: 'bold'
    },
    mediumHeader: {
        fontSize: 20,
        marginBottom: 20,
        marginTop: 20
    },
    textInput: {
        height: 40,
        borderColor: 'gray',
        borderWidth: 1
    }
});
